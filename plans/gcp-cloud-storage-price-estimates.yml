# ESTIMATES computed by API Evangelist — not published by the provider. See `note` and `replace_by`.
generated: '2026-09-20'
method: estimated
estimator: 'planning/api-economics/estimate_prices.py — design: ESTIMATION.md'
note: ESTIMATES, not quotes. Computed by API Evangelist from the prices this provider publishes on https://cloud.google.com/storage/pricing
  (transcribed in plans/gcp-cloud-storage-plans-pricing.yml). Nothing here overrides a published price.
  Each estimate names the figure it came from, the formula, any assumption, and the field the provider
  can publish to replace it — publish that and the estimate is retired.
audience_of_source_pricing: developer-api
pricing_basis: usage-other
estimate_count: 2
estimates:
- kind: entry_cost
  grade: A
  grade_meaning: quoted — the page states this figure
  usd_per_month: 0.0
  meaning: the cheapest published paid tier — a floor on the cost of getting in, NOT the price of the
    API alone
  inputs:
  - file: plans/gcp-cloud-storage-plans-pricing.yml
    plan: Autoclass management fee
    price_text: $0.0025 for every 1000 objects stored for 30 days
  replace_by: 'API Commons Plans — plans[].entries[] with type: fee, a numeric `price`, `timeFrame: month`
    and the tier that includes API access stated in `elements`'
- kind: marginal_cost
  grade: C
  grade_meaning: depends on a stated prior — see `priors`
  usd: 0.0
  per: request
  formula: 'C1 capacity-implied: monthly price / (rps x 2,592,000) / utilisation'
  inputs:
  - file: plans/gcp-cloud-storage-plans-pricing.yml
    plan: Autoclass management fee
    price_text: $0.0025 for every 1000 objects stored for 30 days
  - file: rate-limits/
    published_ceiling: 5000 req/s
  replace_by: 'API Commons Plans — plans[].entries[] with type: metered, `metric` (request | token | transaction
    | message), `unit` and a numeric `price`; and FinOps `primaryUnit` + `meters[]`'
  range_usd:
  - 0.0
  - 0.0
  priors:
    utilisation:
      low: 0.001
      point: 0.01
      high: 0.05
    note: share of the published ceiling actually used
